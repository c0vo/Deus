# === LLM ===
# Everything goes through OpenRouter over its OpenAI-compatible API, so this is
# the only SDK. google-genai is deliberately absent: nothing imports it now.
# The floor is set because config/llm.py relies on extra_body passthrough and
# on `cost` surviving on the usage object.
openai>=1.60.0

# === Embeddings ===
# Also OpenRouter — google/gemini-embedding-001, pinned by MODEL_EMBEDDING to
# keep the stored 3072-dim corpus valid.
numpy
sqlite-vec

# === Telegram Bot ===
python-telegram-bot>=21.0

# === Web Framework ===
fastapi>=0.115.0
uvicorn[standard]>=0.30.0

# === HTTP Client (async) ===
httpx>=0.27.0

# === Web Search ===
tavily-python>=0.5.0

# === News Feeds ===
feedparser>=6.0.0

# === HTML Parsing (for Nitter scraping) ===
beautifulsoup4>=4.12.0

# === Scheduling ===
APScheduler>=3.10.0

# === Configuration ===
pydantic>=2.7.0
pydantic-settings>=2.3.0
python-dotenv>=1.0.0

# === Logging ===
structlog>=24.1.0

# === News APIs ===
finnhub-python>=2.4.0

# === ML Prediction ===
scikit-learn
joblib
yfinance
pandas
langgraph

# === Testing ===
pytest>=8.0.0
pytest-asyncio>=0.23.0
tzdata>=2024.1
